void main() {
  int calification = 15;
  if (calification >= 10) {
    print('Aprobaste con la calificacion de: $calification');
  } else {
    print('Reprobaste con la nota de: $calification');
  }
}
//Todo: Dado un int que representa una calificación (0–20), muestra “Aprobado” si es mayor o igual que 10, de lo contrario “Reprobado”.
/// Se inicializo la variable calification con un valor de 15.
/// declaramos una estructura condicional primeramente valida si la condicion de calification es mayor o igual a 15 imprime aprobado si no reprobado.