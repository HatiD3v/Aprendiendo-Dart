void main() {
  int parImpar = 4;
  if (parImpar % 2 == 0) {
    print('El numero es par: $parImpar');
  } else {
    print('El numero es impar: $parImpar');
  }
}

//Todo:Dado un número entero, determina si es par o impar.
/// declaramos e inicializamos la variable parImpar con algun numero de preferencia.
/// declaramos la condicional if else validando si el numero de la variable parImpar deja residuo al dividirla entre 2, si no deja residuo el numero es par, pero si deja residuo el numero es impar
